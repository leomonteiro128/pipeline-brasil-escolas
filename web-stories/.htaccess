# Brasil Escolas — web-stories directory
# O routing desta pasta é feito pela regra no .htaccess raiz.
# Este arquivo desativa o RewriteEngine localmente para evitar
# conflito com .htaccess legado do WordPress/Drupal Web Stories.
Options -Indexes
RewriteEngine Off
