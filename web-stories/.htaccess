RewriteEngine On
RewriteBase /web-stories/

# index.html em si: servir sem reescrita
RewriteRule ^index\.html$ - [L]

# Slug sem ponto (ex: story-meu-titulo) → index.html
# [a-z0-9-] exclui "." evitando loop de reescrita
RewriteRule ^[a-z0-9][a-z0-9-]+/?$ index.html [L]
