# Brasil Escolas — web-stories directory
# RewriteEngine On para herdar regras do .htaccess raiz
RewriteEngine On
Options -Indexes

# Rota de diagnostico — remover apos confirmar funcionamento
RewriteRule ^htaccess-ok$ /web-stories/index.html [L]
