RewriteEngine On
RewriteBase /web-stories/

# Qualquer slug → index.html (ANTES de verificar se arquivo/dir existe)
# Exclui arquivos com extensão (css, js, png, etc.) e o index.html em si
RewriteRule ^index\.html$ - [L]
RewriteRule ^[^.]+/?$ index.html [L]
